name: Flake checker

on:
  push:
    branches:
      - main
  schedule:
    - cron: "37 13 * * *"
  workflow_dispatch:

jobs:
  flake-checker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: DeterminateSystems/flake-checker-action@v12
